[project]
name = "rabbit-lab"
version = "0.1.0"
description = "RabbitMQ / Celery / Redis messaging & caching examples"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "celery>=5.6.3",
    "pika>=1.4.1",
    "redis>=8.0.1",
    "requests>=2.34.2",
]
